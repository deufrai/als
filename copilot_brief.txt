our task context :

We are building the next version of the website for the software called ALS (Astro live stacker) 

We are using hugo with docsy theme.
docsy is setup in themes/docsy folder as a Git submodule and it checks out the v0.11.0 tag.

project structure is $(git ls-files)

hugo site is set to use 2 languages : FR & EN. FR being the default.
we use docsy's language picker set for dispaying as drop-down
