default:
  interruptible: true

variables:
  GIT_STRATEGY: clone

Deploy website to preprod:
  stage: deploy
  tags: [infra]
  only:
    - release/0.7
  script:
    - ./ci/deploy/deploy_website_to_preprod.sh
  environment:
    name: preprod
    url: https://preprod.als-app.org/
