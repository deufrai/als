default:
  interruptible: true

stages:
  - deploy

variables:
  GIT_STRATEGY: clone

deploy_to_staging:
  stage: deploy
  tags: [infra]
  only:
    - release/1.0
    - master
  script:
    - ./ci/deploy/deploy_staging_website.sh
  environment:
    name: staging
    url: https://next.als-app.org/
